<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mugenunagi.amalbum.datamodel.dao.SampleMapper">
	<!-- テンプレ
	<select id="（メソッド名）" parameterType="（パラメータの型）" resultType="（結果の型）">
		select * from sample where id = #{id}
	</select>
 
	<update id="（メソッド名）" parameterType="（パラメータ名）">
		update sample set value = #{value} where id = #{id}
	</update>

	<delete id="（メソッド名）" parameterType="（パラメータ名）">
		delete from sample where id = #{id}
	</delete>
	-->

	<insert id="insertDummy">
		insert into sample values( 1 , 'test' )
	</insert>
</mapper>
